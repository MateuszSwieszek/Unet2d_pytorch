pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh "./start_file.sh"
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
    }
}