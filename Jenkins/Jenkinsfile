pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh "${REPO_PATH}/Jenkinsstart_file.sh"
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
    }
}